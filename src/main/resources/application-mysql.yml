spring:
  config:
    activate:
      on-profile: mysql

  datasource:
    url: jdbc:mysql://srv1670.hstgr.io:3306/u229745468_comparemydevic?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC&createDatabaseIfNotExist=true&useUnicode=true&characterEncoding=utf8
    username: u229745468_comparemydevic
    password: H@YGX3*w
    driver-class-name: com.mysql.cj.jdbc.Driver
    hikari:
      maximum-pool-size: 10
      minimum-idle: 2
      idle-timeout: 30000
      connection-timeout: 30000

  jpa:
    open-in-view: false
    hibernate:
      ddl-auto: none   # Hibernate won’t touch schema; Flyway will
    show-sql: true
    properties:
      hibernate:
        format_sql: true
    database-platform: org.hibernate.dialect.MariaDBDialect   # use MySQL8Dialect if your DB is MySQL 8

  flyway:
    enabled: true
    locations: classpath:db/migration-mysql
    baseline-on-migrate: true
    validate-on-migrate: false
    repair-on-migrate: true
    clean-disabled: false
    clean-on-validation-error: true
    # For one-time reset in dev ONLY (dangerous in prod):
    # clean-disabled: false

server:
  port: 8080

logging:
  level:
    org.springframework: INFO
    org.hibernate.SQL: WARN
    org.flywaydb: INFO